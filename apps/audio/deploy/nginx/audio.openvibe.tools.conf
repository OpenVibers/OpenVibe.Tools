# ═══════════════════════════════════════════════════════════════
# Audio.OpenVibe — Nginx Vhost
# Single backend serving all audio format + tool subdomains.
# ═══════════════════════════════════════════════════════════════

limit_req_zone $binary_remote_addr zone=openvibeaudio_api:10m rate=5r/s;

# HTTP → HTTPS redirect
server {
    listen 80;
    listen [::]:80;
    server_name
        audio.openvibe.tools
        mp3.openvibe.tools wav.openvibe.tools flac.openvibe.tools
        ogg.openvibe.tools m4a.openvibe.tools aac.openvibe.tools
        opus.openvibe.tools wma.openvibe.tools aiff.openvibe.tools ac3.openvibe.tools
        trim.openvibe.tools merge.openvibe.tools
        pitch.openvibe.tools speed.openvibe.tools reverse.openvibe.tools
        normalize.openvibe.tools fade.openvibe.tools loop.openvibe.tools
        bass.openvibe.tools equalizer.openvibe.tools
        vocal.openvibe.tools karaoke.openvibe.tools
        extract.openvibe.tools waveform.openvibe.tools
        ringtone.openvibe.tools podcast.openvibe.tools
        voice.openvibe.tools noise.openvibe.tools
        bitcrusher.openvibe.tools echo.openvibe.tools
        reverb.openvibe.tools chorus.openvibe.tools
        distortion.openvibe.tools compressor.openvibe.tools
        stereo.openvibe.tools silence.openvibe.tools
        metadata.openvibe.tools convert.audio.openvibe.tools;

    return 301 https://$host$request_uri;
}

# HTTPS
server {
    listen 443 ssl;
    listen [::]:443 ssl;
    http2 on;

    server_name
        audio.openvibe.tools
        mp3.openvibe.tools wav.openvibe.tools flac.openvibe.tools
        ogg.openvibe.tools m4a.openvibe.tools aac.openvibe.tools
        opus.openvibe.tools wma.openvibe.tools aiff.openvibe.tools ac3.openvibe.tools
        trim.openvibe.tools merge.openvibe.tools
        pitch.openvibe.tools speed.openvibe.tools reverse.openvibe.tools
        normalize.openvibe.tools fade.openvibe.tools loop.openvibe.tools
        bass.openvibe.tools equalizer.openvibe.tools
        vocal.openvibe.tools karaoke.openvibe.tools
        extract.openvibe.tools waveform.openvibe.tools
        ringtone.openvibe.tools podcast.openvibe.tools
        voice.openvibe.tools noise.openvibe.tools
        bitcrusher.openvibe.tools echo.openvibe.tools
        reverb.openvibe.tools chorus.openvibe.tools
        distortion.openvibe.tools compressor.openvibe.tools
        stereo.openvibe.tools silence.openvibe.tools
        metadata.openvibe.tools convert.audio.openvibe.tools;

    ssl_certificate     /etc/letsencrypt/live/openvibe.tools/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/openvibe.tools/privkey.pem;

    access_log /var/log/nginx/audio.openvibe.tools.access.log;
    error_log  /var/log/nginx/audio.openvibe.tools.error.log;

    # Allow larger uploads for audio/video files
    client_max_body_size 100m;

    # Security headers
    add_header X-Content-Type-Options nosniff always;
    add_header X-Frame-Options SAMEORIGIN always;
    add_header Referrer-Policy strict-origin-when-cross-origin always;

    # API routes (rate limited)
    # Prometheus metrics are for loopback scrapes only (the app also 404s any proxied caller).
    location = /metrics {
        return 404;
    }

    location /api/ {
        limit_req zone=openvibeaudio_api burst=20 nodelay;

        proxy_pass http://127.0.0.1:4014;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # Longer timeout for audio processing
        proxy_read_timeout 180s;
        proxy_send_timeout 180s;
    }

    # Static assets + SPA fallback
    location / {
        proxy_pass http://127.0.0.1:4014;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # Deny dotfiles
    location ~ /\.(?!well-known) {
        deny all;
    }
}
